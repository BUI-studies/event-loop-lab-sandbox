/**
 * English is the source of truth for the key vocabulary: every other locale is
 * typed as `Record<TranslationKey, string>`, so a missing key is a build error
 * rather than a silent fallback.
 *
 * Key prefixes: `p*` phase messages, `t*` loop-trace lines, `i*` item labels
 * shown inside the queue/heap/Web API panels, `preset*` preset names.
 */
export const en = {
  title: 'Event Loop Lab',
  sub: 'Write JavaScript, then step the loop one action at a time. Timers, promises and fetch are replaced with instrumented versions, so every queue push and pop is visible.',
  yourcode: 'Your code',
  load: 'Load into loop',
  step: 'Step',
  play: 'Play',
  pause: 'Pause',
  finish: 'Run to idle',
  edit: 'Edit',
  reset: 'Reset',
  pace: 'pace',
  console: 'Console',
  trace: 'Loop trace',
  stack: 'Call stack',
  web: 'Web API - off the JS thread',
  micro: 'Microtask queue - drains fully before the next task',
  macro: 'Task queue',
  heap: 'Heap - promise objects and paused async functions',
  emptyStack: 'empty - the thread is free',
  emptyQueue: 'empty',
  emptyWeb: 'no pending timers or requests',
  emptyHeap: 'nothing allocated yet',
  emptyConsole: 'Nothing logged yet.',
  lines: 'lines',
  startHint: 'Load code to begin.',
  legStack: 'call stack',
  legTask: 'task',
  legMicro: 'microtask',
  legWeb: 'web API',
  legHeap: 'heap',
  note: 'Write your own code and press Load. <code>async</code>/<code>await</code> is parsed and rewritten into a generator state machine before it runs, so each <code>await</code> becomes a visible pause: the frame leaves the stack, the paused state stays on the heap, and a resume closure waits inside the promise you awaited. Declarations, function expressions, arrow functions, object methods and top-level <code>await</code> all work, alongside <code>setTimeout</code>, <code>setInterval</code>, <code>queueMicrotask</code>, <code>Promise.all</code> and <code>requestAnimationFrame</code>. <code>async</code> methods on a class are the one gap, and the lab says so instead of failing quietly. <code>fetch</code> is mocked - <code>/users</code> returns three users, <code>/dossier/&lt;id&gt;</code> returns one record. The blue line is where execution stopped this step; violet lines also ran during it.',

  pScript: 'Script queued as the first task.',
  pTaskTaken: 'Task taken: {label} -> pushed on the call stack.',
  pTaskDone: 'Task finished: {label} -> frame popped.',
  pMicroTaken: 'Microtask taken: {label}{queued}',
  pMicroQueued: ' ({n} still queued)',
  pMicroMore: 'Done - but it queued more. Draining continues before any task runs.',
  pMicroEmpty: 'Microtask queue empty after {n} job(s).',
  pRenderRun: 'Render step: running {n} animation frame callback(s).',
  pRenderDone: 'Painted.',
  pPark: 'Every queue is empty and nothing is pending. The thread is free, with nothing left to do.',
  pSleep: 'Nothing queued, so the thread is free, not blocked. It sleeps {ms}ms at 0% CPU while the Web API works, and runs anything that arrives the moment it arrives.',
  pUnhandled: 'Microtasks drained, and {n} rejection(s) had nobody listening.',
  pWoken: 'Woken by: {what}.',
  pIdle: 'Idle. Nothing left in any queue.',

  tDrain: '{label} settled {state}, draining {n} reaction(s)',
  tRanTask: 'ran task: {label}',
  tRanMicro: 'ran microtask: {label}',
  tPainted: 'painted',
  tWebDone: 'web API finished: {label} -> queued a task',
  tUnhandled: 'unhandled rejection(s): {n}',

  pCompileFailed: 'That code did not compile. The console says why.',
  iSyntaxError: 'Line {line}: {message}',
  iCompileError: 'Compile error: {message}',
  iUnsupported: 'Line {line}: {what} is not supported yet.',
  iClassMethod: 'async methods on a class',
  iComputedMethod: 'this method form',

  iScript: 'initial script',
  iScriptMeta: 'whole script body',
  iTimer: 'timer {ms}ms',
  iTimerMeta: 'os timer thread',
  iTimerCb: 'setTimeout cb ({ms}ms)',
  iInterval: 'interval {ms}ms',
  iIntervalCb: 'setInterval cb ({ms}ms)',
  iTimerId: 'timer #{id}',
  iNet: 'network {url}',
  iNetMeta: '{ms}ms round trip',
  iResolve: 'resolve {label}',
  iHeaders: 'response headers in',
  iBodyRead: 'body read',
  iParse: 'body parse {url}',
  iParseMeta: 'stream read',
  iRaf: 'animation frame callbacks',
  iRafMeta: '{n} queued',
  iQmt: 'queueMicrotask cb',
  iQmtMeta: 'manual',
  iReaction: 'reaction',
  iOn: 'on {label}',
  iSettled: '{label} (already settled)',
  iResume: '{name}() resume',
  iStep: 'step {n}',
  iPaused: '{name}() paused',
  iAwaiting: 'awaiting · locals preserved across the pause',
  iStart: 'start',
  iWaiting: '{n} reaction(s) waiting',
  iValue: 'value: {v}',
  iFires: '{meta} · fires at {t}ms',
  iUncaught: 'Uncaught {error}',
  // Left in English in every locale: it is what a real console prints.
  iUnhandledRejection: 'Uncaught (in promise) {error}',

  presetP1: 'Ordering: sync, task, microtask',
  presetP2: 'Microtasks jump the queue',
  presetP3: 'await pauses and resumes',
  presetP4: 'Two awaits, sequential fetches',
  presetP5: 'Promise.all - parallel dossiers',
  presetP6: 'Timers vs promises vs rAF',
  presetP7: 'The thread is free during a fetch',
  presetCustom: 'Your own code',
};

export type TranslationKey = keyof typeof en;
